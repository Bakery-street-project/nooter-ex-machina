# ── Secrets & credentials ──
.env
.env.*
*.pem
*.key
*.cert
*.p12
*.pfx
*.jks
*.keystore
secrets/
credentials/
*.secret
*.credentials
token.json
tokens.json
service-account*.json

# ── API keys / config with secrets ──
wrangler.toml
.dev.vars
*.local

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Node ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# ── Build / dist ──
dist/
build/
out/
*.min.js
*.map

# ── Archives (don't commit source bundles) ──
*.tar.gz
*.zip
*.gz
*.bz2
*.7z

# ── Logs ──
*.log
logs/

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── Docker ──
.dockerignore
docker-compose.override.yml
